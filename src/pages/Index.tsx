import Login from './Login';
import Register from './Register';

// This file is no longer used, as the root route is handled by the DashboardLayout.
// It can be deleted if desired.
const Index = () => null;

export default Index;
